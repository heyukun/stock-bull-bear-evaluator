# 股票多空因子评估专家 (stock-bull-bear-evaluator)

对用户输入的公司 / 股票进行 **14 个因子的系统性多空评估**（估值因子区分成长/价值/周期股方法），覆盖 A 股（sh/sz/bj）与港股（hk）。

当用户说「评估 XX 的多空」「XX 多空分析」「分析 XX 股票多空因子」「多空评估：公司名」时触发。

## 核心能力

- **14 因子三时间框架评估**：每个因子独立给出「当前多空 / 中期展望(3-6M) / 长期评估(3Y)」三个判断，避免混为一谈。
- **前置筛选关卡**：全量评估前四道关卡（高权重因子利空筛查 + 长期整体评分 ≥7 分），任一触发即终止流程。
- **尾部风险熔断**：偿债/审计/质押/客户集中度四项红线，触发任一项综合多空评分记 -1（评级同时封顶「看空」）。
- **利空因子深挖**：针对利空 / 中性偏空因子，寻找公司应对措施、定义可量化跟踪指标，并通过网络搜索验证最新进展。
- **加权多空评分**：1-10 分制、三时间框架加权、五档评级，由脚本自动计算，保证可追溯。

## 目录结构

```
stock-bull-bear-evaluator/
├── SKILL.md                      # Skill 主流程与关键纪律（必读）
├── config/
│   └── scoring-config.json       # 评分参数唯一权威配置（权重/阈值/档位分值）
├── references/
│   ├── factor-definitions.md     # 14 因子多/空判定标准与所需数据
│   ├── momentum-check.md         # 因子 13（业绩动能）14 项检查规则
│   ├── scoring-algorithm.md      # 加权评分算法规格（分值映射/权重/阈值）
│   ├── data-sources.md           # 结构化因子与熔断项所需数据清单
│   └── source-credibility.md     # T0-T5 信源可信度分级标准
├── scripts/
│   └── scoring_calc.py           # 三时间框架加权评分脚本（仅依赖标准库）
└── assets/
    └── report-template.md        # .md 评估报告输出模板
```

## 快速开始

### 评分计算（强制使用脚本）

```bash
python3 scripts/scoring_calc.py <评估输入.json>
```

输入 JSON 为 14 因子的三框架档位（可选 `circuit_breakers` 熔断字段），脚本自动校验配置并计算加权总分、输出评分表。

> 脚本仅依赖 Python 3 标准库，无需安装第三方依赖。

## 输出物

对话内输出三张表（14 因子三时间框架评估表 / 利空应对指标跟踪表 / 加权多空评分表），并生成 `多空评估报告_<公司名>_<YYYYMMDD>.md` 文件。

## 关键纪律（摘要）

1. 每个因子必须给出明确多空判断，不可只列现象。
2. 利空因子必须有应对措施分析，无则如实报告「未公开明确应对措施」。
3. 信源须 T2+ 才可引用，T4/T5（雪球、贴吧、KOL）不引用。
4. 加权评分必须由 `scoring_calc.py` 计算，不可手工估算。
5. 评分参数以 `config/scoring-config.json` 为唯一权威，调整须记录 changelog。
6. 报告末尾必须附免责声明，本工具不构成投资建议。
7. 尾部风险熔断不可跳过，触发任一项综合多空评分记 -1（评级同时封顶「看空」）。
8. 前置筛选关卡一票否决，任一触发即终止全部流程。

详细流程与全部 20 条纪律见 [SKILL.md](./SKILL.md)。

## License

本仓库仅供个人 / 研究使用，不构成任何投资建议。
