---
name: stock-bull-bear-evaluator
slug: stock-bull-bear-evaluator
displayName: 股票多空因子评估专家
description: 股票多空因子评估专家。当用户输入一个公司名或股票名（如"比亚迪"、"腾讯控股"、"分析XX的多空"），对该公司进行 14 个因子的系统性多空评估（估值因子区分成长/价值/周期股方法），并针对利空因子寻找公司应对措施、定义可量化跟踪指标、通过网络搜索获取最新进展并给出评价（超出预期/符合预期/不及预期）。含尾部风险熔断检查（偿债/审计/质押/客户集中度，触发即封顶综合评级）与一致预期差校验。全量评估前设五道前置筛选关卡（高权重因子利空筛查 + 当前/长期整体评分 ≥7 分），任一关卡触发即终止全部流程。覆盖 A 股与港股。触发场景：用户说"评估XX的多空""XX多空分析""分析XX股票多空因子""多空评估：公司名"等。
agent_created: true
---

# 股票多空因子评估专家

## 概述

对用户输入的公司/股票，系统性评估 14 个因子的多空倾向（三时间框架：当前 / 未来 3-6 个月 / 未来 3 年），针对利空因子深挖公司应对措施与可量化跟踪指标，并通过网络搜索验证指标最新进展。其中因子 14（估值）须先判定公司类型（成长/价值/周期）再选主估值指标。输出对话内多空评估表 + 利空应对指标跟踪表 + 加权多空评分表（1-10 分制、三时间框架加权、五档评级），同时生成完整 .md 评估报告文件。评分前强制执行尾部风险熔断检查（偿债/审计/质押/客户集中度），触发任一项综合评级封顶"看空"；因子 13 须与卖方一致预期做预期差校验。**进入全量评估前，须先通过第 0.5 步五道前置筛选关卡，任一关卡触发即终止全部流程。**

**适用市场**：A 股（sh/sz/bj）、港股（hk）。

## 触发与意图识别

触发词示例：
- "多空评估：比亚迪" / "评估腾讯控股的多空"
- "分析 XX 股票的多空因子"
- "XX 多空分析" / "帮我看下 XX 的多空"

输入为公司名或股票名时，按本 skill 流程执行。若用户只问单一指标（如"比亚迪融资余额多少"），不触发本 skill，直接用对应数据源回答。

## 工作流程

> **流程起点**：任何评估与搜索必须始于下方「前置步骤：实时时间锚定与最新财报状态确认」。先确认真实评估时点与最新财报，再进入标的解析，否则后续所有时间相关判断都会失真。

### 前置步骤：实时时间锚定与最新财报状态确认（强制 · 最先执行 · 不可跳过）

在调用任何评估动作、运行任何网络搜索之前，必须首先完成本步，确立本次评估的「真实评估时点」与「最新财报基准」。**禁止**使用模型记忆中的日期、上一会话遗留的日期，或假设「数据就是最新的」。

1. **获取实时时间（严禁使用模型记忆中的"今天"）**
   - 运行命令获取当前最真实的系统时间，禁止用模型内置记忆的日期或上一会话遗留的日期：
     ```bash
     date "+评估基准日：%Y 年 %m 月 %d 日（%A），当前 %H:%M（本地时区）"
     ```
   - 若系统时间受限 / 不可信，回退到可信时间服务：
     ```bash
     curl -s "https://worldtimeapi.org/api/timezone/Asia/Shanghai"
     ```
   - 将结果作为本次评估的**评估基准日**，全程沿用；后续所有搜索词、财报期判断、季报披露节奏均以此为准。

2. **确认公司最新定期报告状态（基于实时时间，禁止凭记忆）**
   - 基于「评估基准日」，用网络搜索确认该公司**已发布的最新一期定期报告**是哪一报告期（年报 / 一季报 / 中报 / 三季报）、其实际披露日期、报告期截止日。
   - 搜索词**必须携带实时年份 / 月份**（来自步骤 1 的评估基准日），例如：
     - `比亚迪 2026 一季报 年报 披露时间`
     - `腾讯控股 2026 中期业绩 发布日期`
   - 同时确认：当前处于哪个财报真空期、下一期报告预计披露时间、是否已有业绩预告 / 业绩快报。
   - **严禁**使用模型记忆中的历史财报期（如"我记得它去年发了年报"）或假设"数据就是最新的"——必须以实时搜索结果为准。

3. **锚定财报数据时效，向下传递**
   - 将确认结果（评估基准日 + 最新已发布报告期 + 披露日期）作为各因子评估、第 2 步跟踪指标数据时效、第 4 步报告标注的输入：
     - 财务数据相关的因子判断必须基于上述「最新一期已发布报告」，不得沿用上一期数据（呼应纪律第 13 条「季报发布即刷新」）。
     - 报告文件中须显式标注「评估基准日」与「所用财报期 / 报告发布日期」。

**为什么必须前置**：所有时间相关判断（财报披露节奏、中期 3-6 个月窗口、搜索词时间范围）都依赖真实"今天"。若用模型记忆的过期时间或沿用上一会话的日期，会导致搜索词带错年份、遗漏刚发布的最新财报、对"是否已披露 / 处于真空期"判断错误，直接动摇多空结论的可靠性。

### 第 0 步：解析标的与市场

1. 用 WeStock 搜索解析公司名 → 股票代码：
   ```bash
   npx -y westock-data-clawhub@1.0.4 search 比亚迪
   ```
2. 确认代码前缀：A 股 `sh/sz/bj`，港股 `hk`。
3. 用 `profile` 取公司简况（行业、主营、上市日期），作为后续分析基础。

### 第 0.5 步：多空前置筛选关卡（强制 · 在第 1 步前执行 · 任一关卡触发即终止全部流程）

在进入第 1 步全量评估前，按以下顺序执行五道前置关卡，快速剔除高权重因子已明显恶化或整体评分不达标的标的。**任一关卡触发即直接结束全部流程**：不再执行后续任何步骤，仅在对话内输出「终止说明」（触发的关卡、已评估因子的档位与关键证据、简要结论），不生成完整评估报告文件（用户明确要求时除外）。

**关卡因子取数规则**：各框架的"前 N 大权重因子"以 [config/scoring-config.json](./config/scoring-config.json) 对应框架的 `factor_weights` 动态取权重最高者（呼应纪律第 17 条，权重以配置为唯一权威）；权重并列时因子序号小者优先。按当前配置（v1.3.0）：

| 框架 | 前 N 大权重因子 |
|------|----------------|
| 当前（前 4） | 行业（10%）、下游（10%）、资金多空（10%）、业绩动能（8%） |
| 中期（前 3） | 行业（11%）、下游（11%）、估值（10%） |
| 长期（前 3） | 行业（14%）、下游（12%）、竞争对手（11%） |

**关卡 1：当前前 4 大权重因子筛查**
- 仅评估当前框架前 4 因子的**当前多空**档位（判定标准同第 1 步与 factor-definitions.md；业绩动能须先运行 `scripts/momentum_calc.py`）。
- **触发条件：≥ 2 个因子判「利空」→ 终止全部流程。**
- 未触发 → 进入关卡 2。（「中性偏空」不计入触发数，但须如实呈现。）

**关卡 2：中期前 3 大权重因子筛查**
- 评估中期框架前 3 因子的**中期展望（3-6 个月）**档位。
- **触发条件：任一因子判「不佳」（中期框架的利空档）→ 终止全部流程。**
- 未触发 → 进入关卡 3。

**关卡 3：长期前 3 大权重因子筛查**
- 评估长期框架前 3 因子的**长期评估（3 年）**档位。
- **触发条件：任一因子判「利空」→ 终止全部流程。**
- 未触发 → 进入关卡 4。

**关卡 4：当前多空整体评分 ≥ 7 分**
- 完成全部 14 因子的**当前多空**档位判定（含因子 13 业绩动能全量检查与 13a 预期差校验），将当前框架档位代入 `scoring_calc.py` 计算**当前框架得分**（其余框架已评估部分如实填入、未评估部分由脚本按中性计；本关卡只读取当前框架得分）。
- **触发条件：当前框架得分 < 7 → 终止全部流程。**
- ≥ 7 → 进入关卡 5。

**关卡 5：长期多空整体评分 ≥ 7 分**
- 完成全部 14 因子的**长期评估（3 年）**档位判定，同样以脚本计算**长期框架得分**。
- **触发条件：长期框架得分 < 7 → 终止全部流程。**
- ≥ 7 → 通过全部前置关卡：继续执行第 1 步（补齐中期框架全量判定）及后续全部流程（第 1.5 步单位经济、第 1.6 步尾部风险熔断、第 1.7 步三框架综合评分、第 2-4 步利空跟踪与输出）。

**终止说明输出格式**（任一关卡触发时）：

| 项目 | 内容 |
|------|------|
| 触发关卡 | 关卡 X（名称） |
| 已评估因子档位 | 因子 - 档位 - 关键证据（附数据日期与信源） |
| 评分（仅关卡 4/5） | 当前/长期框架得分与阈值 7 分对比 |
| 结论 | 因 XX 触发前置关卡，终止后续评估流程 |

### 第 1 步：14 因子三时间框架多空评估

> **衔接说明**：通过第 0.5 步前置关卡后，14 因子的当前多空与长期评估两列已在关卡 4/5 完成，本步只需**补齐中期展望列的全量判定**（关卡 2 已评估的前 3 因子直接沿用），并复核汇总三列结果。

对下表 14 个因子逐一进行**三时间框架评估**，三个判断独立给出，不可互相替代：

- **当前多空**：基于当前事实判断，取值 `利多 / 利空 / 中性 / 中性偏多 / 中性偏空`
- **中期展望**：基于**未来 3-6 个月**该因子的变动方向判断，取值 `向好 / 不明朗 / 不佳`
- **长期评估**：基于**未来 3 年**该因子的方向判断，取值 `利多 / 利空 / 中性 / 中性偏多 / 中性偏空`（判定指引见 [references/scoring-algorithm.md](./references/scoring-algorithm.md) 第 5 节；筹码/资金/舆论/业绩动能等短期因子长期基本不适用，默认"中性"）
  - **向好（多）**：未来 3-6 个月该因子方向明显改善（不关注绝对值，仅关注方向）
  - **不明朗（中性）**：未来方向不确定，或无可靠前瞻信号，或多空信号并存无法判断
  - **不佳（空）**：未来 3-6 个月该因子方向明显恶化

**为什么选 3-6 个月**：该窗口与季报披露节奏匹配（1-2 个季度），可被后续财报验证，避免过度推断。

**多框架并列的典型场景**（合法且常见，须如实标注）：
- 当前利多 + 中期展望不佳：景气仍在高位但拐点临近（如产能即将集中释放）
- 当前利空 + 中期展望向好：底部已现、拐点将至（如库存去化接近尾声）
- 当前中性 + 中期展望不明朗：无可靠前瞻信号时如实标注，不强行判断
- 中期承压 + 长期利多：短期扰动不改 3 年结构性方向（如周期底部）

每个因子附 1-2 句简要说明（含关键数据或事件），需**同时覆盖当前事实依据与中期/长期判断依据**。

**因子定义与三时间框架判定标准**：见 [references/factor-definitions.md](./references/factor-definitions.md)（必读，包含每个因子的当前多/空标准 + 中期向好/不佳标准与所需数据）；长期（3 年）判定指引见 [references/scoring-algorithm.md](./references/scoring-algorithm.md) 第 5 节。

**数据源策略**（混合数据源）：
- **定性因子**（宏观/社会舆论/行业/政策/上游/下游/竞争对手/替代品/进入壁垒）：用元宝搜索标准版（`tencent-yuanbao-standard-search` skill），不可用时回退 WebSearch。
- **结构化因子**（公司资本操作/筹码/资金/业绩动能）：用 WeStock CLI（`npx -y westock-data-clawhub@1.0.4`）取结构化数据，具体命令见 [references/data-sources.md](./references/data-sources.md)。
- **估值因子（因子 14）**：主指标历史分位与可比公司估值用元宝搜索（理杏仁/乐咕乐股/券商研报等 T2+ 来源）；预期增速与 13a 共用 NeoData 一致预期。取数方式见 data-sources.md 第 6 节。
- **机构观点/研报/一致预期**：用 NeoData 金融搜索（`neodata-financial-search` skill）补充；因子 13 的 13a 预期差校验须用其获取卖方一致预期。

**信源可信度**：所有结论引用 T2+ 信源，T4/T5 不引用。标准见 [references/source-credibility.md](./references/source-credibility.md)。

**因子 13（业绩动能）评估**：对最新季度财务数据进行 14 项多季度趋势检查（定义与预警规则见 [references/momentum-check.md](./references/momentum-check.md)，必读），按 factor-definitions.md 第 13 节的映射规则将预警结果汇总为因子 13 的当前多空档位；同时必须执行 13a 预期差校验（与卖方一致预期对照，定义见 factor-definitions.md 第 13a 节）。

**自动化脚本**（推荐）：

```bash
python3 scripts/momentum_calc.py <stock_code>
```

脚本自动获取三大报表、计算所有指标与比率、检测趋势、输出预警报告。仅依赖 Python 标准库。若脚本不可用，手动从 WeStock `finance` 命令获取数据并按 momentum-check.md 定义的规则计算；港股不支持时按 momentum-check.md「港股特殊处理」执行。

### 第 1.5 步：单位经济与量价拆分（通用分析镜头，贯穿第 1 步因子评估）

在第 1 步因子评估的同时，强制执行一次**行业无关的单位经济拆解**，作为连接定性判断与财务数据的分析镜头：

1. **识别核心单位**：为该公司选定一个最具代表性的"单位"（如产品件/吨/千瓦时/订阅用户/订单/客单/座位/房间夜等），由分析者根据公司业务自行确定，框架不预设。
2. **量价拆分**：将收入增长分解为"量（出货/销量/用户数）× 价（单价/ARPU/客单价）"两部分，识别增长由量驱动还是价驱动，以及量增价跌/量价齐升等组合含义。
3. **单位盈利趋势**：跟踪"单位毛利""单位净利"的趋势，捕捉"总收入/总利润增长但单位盈利被侵蚀"的隐蔽恶化（绝对额增长 ≠ 单位盈利健康）。
4. **传导校验**：当上游涨价时，用单位售价 vs 单位成本的变动判断传导是否充分（联动可传导 vs 难传导 + 锁价敞口，见因子 5）。

**通用性原则**：本镜头不绑定任何行业或公司。若某公司确无合理"单位"指标（罕见），则标注"单位经济不适用"并以"收入=量×价"中可获取的维度替代，不得跳过分析。

### 第 1.6 步：尾部风险熔断检查（强制 · 评分前置）

加权评分是 14 因子的加权平均，**致命风险可能被 13 个利多因子稀释掉**。因此在评分前必须逐项检查以下 4 类熔断项（偿债数据复用因子 13 已取的资产负债表/现金流量表；其余查年报/交易所公告，T0 信源优先，取数方式见 data-sources.md 第 5 节）：

| # | 熔断项 | 触发条件（任一即触发） |
|---|--------|------------------------|
| 1 | 偿债红线 | ① 货币资金 < 短期借款且经营现金流为负；② 流动比率 < 1 且连续 2 季度下降；③ 存贷双高（货币资金与有息负债双高且利息收入/货币资金异常偏低） |
| 2 | 审计与治理红线 | 最新年报审计意见为非无保留意见；或近 24 个月内更换会计师事务所；或被证监会立案调查 |
| 3 | 大股东质押红线 | 控股股东累计质押占其持股比例 > 70%（或 > 50% 且股价已接近平仓线） |
| 4 | 客户集中度红线 | 第一大客户占营收 > 50%（最新年报口径） |

**执行规则**：
1. 逐项检查，触发项必须附证据（数据 + 日期 + 信源）；未触发项标注"未触发"，不可跳过不查。
2. **触发任一项 → 综合评级封顶"看空"**：将触发清单写入 `scoring_calc.py` 输入 JSON 的 `circuit_breakers` 字段（格式见 scoring-algorithm.md 第 6.2 节），脚本自动封顶；定性结论与报告显著位置同步 ⚠️ 标注。
3. 熔断项**不得**以其他因子利多抵消，也不参与加权——它是封顶条件而非第 14 个因子。

### 第 1.7 步：三时间框架加权评分

完成第 1 步因子判定后，将各因子的三框架档位代入加权打分模型，量化为 1-10 分制加权总分与五档评级（强烈看多/看多/中性/看空/强烈看空）。算法规格（分值映射、因子权重、框架权重、阈值）见 [references/scoring-algorithm.md](./references/scoring-algorithm.md)，全部参数集中于 [config/scoring-config.json](./config/scoring-config.json)。

**自动化脚本（强制使用）**：

```bash
python3 scripts/scoring_calc.py <评估输入.json>
```

输入 JSON 为 14 因子的三框架档位（格式见 scoring-algorithm.md 第 6.2 节），脚本自动校验配置（权重和=100%、档位完整、阈值覆盖）、计算各框架得分与综合加权总分、输出可直接嵌入报告的 markdown 评分表。强度微调（档位基准分 ±1）为可选项，须给出依据；回测时关闭微调保证一致性。

### 第 2 步：利空因子应对措施与跟踪指标

对所有判定为**利空 / 中性偏空**的因子：
1. 在公司公开信息（年报、公告、投资者交流、官方 PR）中查找是否有**明确的应对措施**。
2. 若有应对措施 → 定义**可量化跟踪指标**，格式：`{指标}：指标名称`。
3. 若无应对措施 → **如实报告"公司未公开明确应对措施"**，不编造。
4. **数据时效性规则**：跟踪指标必须使用最新可得数据（财务类指标使用**最新季度数据**，非 trailing annual），并在指标旁标注数据日期。

### 第 3 步：跟踪指标最新进展查询

对第 2 步定义的每个跟踪指标：
1. 用元宝搜索标准版（不可用回退 WebSearch）查询该指标最新进展。
2. 给出**进展评价**：超出预期 / 符合预期 / 不及预期。
3. 评价必须基于具体数据或事件，附信源链接。

**搜索纪律**：每个指标单独搜索，不合并查询；查询词要具体（如"比亚迪海外销量 2026"而非"比亚迪海外"），且**必须携带前置步骤锚定的评估基准日年份 / 月份**，禁止使用模型记忆的年份或上一会话的日期；优先 T2+ 信源。每次搜索前默念一次"今天是哪个真实日期"，确保命中最新财报与最新进展。

**强实时信息须精确到"月"（月-1 / 月-2 锚定）**：对强时效性指标（如月度销量、月度交付量、月度订单、月度装机量、周度/月度高频数据等；财报除外——财报按"前置步骤"的报告期锚定），搜索词**不能只笼统带年份**，必须基于评估基准日向下锚定到"当前月 − 1"或"当前月 − 2"，以命中刚出炉的最新一期数据、避免搜到上一年同月或全年累计的旧值。例如评估基准日为 2026 年 8 月时：
- 比亚迪最新月销量 → `比亚迪 2026年7月 销量` 或 `比亚迪 2026年6月 交付量`（而非"比亚迪 2026 销量"）
- 特斯拉上海工厂月出货 → `特斯拉 上海 2026年7月 交付`
- 若某指标官方通常在次月 10 日左右披露，则用"当前月 − 1"；披露更滞后则用"当前月 − 2"。具体回退档位以实时搜索结果的可得性为准，并在指标旁标注数据所属月份。

**年份 + 月-1/月-2 双锚定原则**：时间越强的信息，月份锚定越要贴近"刚刚过去的那一期"，不得以年份笼统替代月份。

### 第 4 步：输出

#### 4.1 对话内输出（三张表）

**表 1：14 因子三时间框架多空评估表**

| 因子 | 当前多空 | 中期展望(3-6M) | 长期评估(3Y) | 简要说明 |
|------|----------|----------------|--------------|----------|
| 宏观 | 利多/利空/中性 | 向好/不明朗/不佳 | 利多/利空/中性 | ... |
| ... | ... | ... | ... | ... |

**表 2：利空应对指标跟踪表**

| 利空因子 | 公司应对措施 | 跟踪指标 | 最新进展 | 进展评价 |
|----------|--------------|----------|----------|----------|
| 下游 | 技术升级提升渗透率 | 闪充站建设目标达成率 | 截至 X 月建成 X 个 | 符合预期 |
| ... | ... | ... | ... | ... |

**表 3：加权多空评分表**（由 `scoring_calc.py` 自动生成，直接粘贴）

含：各时间框架的因子分值×权重明细、三框架得分与框架评级、综合加权总分与综合评级（五档）。对话内可只贴"综合加权总分"汇总表 + 简要框架得分，完整明细进报告文件。

#### 4.2 生成 .md 报告文件

在当前工作目录生成 `多空评估报告_<公司名>_<YYYYMMDD>.md`，使用 [assets/report-template.md](./assets/report-template.md) 模板。报告含：标的概况、14 因子三框架详评、因子 13 业绩动能检查明细、加权多空评分、利空应对与指标跟踪、综合多空判断、信源清单、免责声明。

## 关键纪律

1. **每个因子必须给明确多空判断**，不可只列现象不下结论。
2. **利空因子必须有应对措施分析**：有则给指标，无则如实报告"未公开明确应对措施"。
3. **跟踪指标进展评价必须基于具体数据/事件**，不可泛泛而谈。
4. **信源 T2+ 才可引用**，T4/T5（雪球、贴吧、KOL）不引用。
5. **港股特殊性**：筹码数据 WeStock 不支持（仅沪深京 A 股），港股筹码多空用 web search 查机构持仓变化；港股资金多空的 call/put 用 web search 查。
6. **数据时效**：所有数据标注日期；新闻类信源优先近 3 个月。
7. **中立立场**：多空判断基于事实，不预设立场；利多利空并存时如实标注。
8. **不构成投资建议**：报告末尾必须附免责声明。
9. **货币单位**：港股数据为港元/美元，展示时标注正确货币单位，A 股为人民币。
10. **结构性判断与短期业绩背离时必须标注**：当结构类因子（1-12 及 14）整体判断为"利多/中性偏多"但因子 13（业绩动能）判"中性偏空/利空"时，必须在最终结论中明确标注"结构性判断与短期业绩存在背离"，并建议跟踪后续季度业绩是否企稳。
11. **框架通用性（强制）**：本框架适用于 A 股/港股任意行业。所有因子定义、检查项、分析镜头均不得绑定特定公司或行业的方法。涉及"单位""产能""供给能力指标"等概念时，由分析者按公司业务性质对应替换（如产能→人员/算力/门店等），框架不预设行业专属指标。
12. **政策因子必须双向分列**：政策因子须同时列出支持项与打压项（含税费新增/恢复征收、补贴退坡、监管收紧等），分别评估后汇总。严禁只抓一端而漏掉反向政策（如只见反内卷利多而忽略同期税费利空）。
13. **季报发布即刷新**：最新一期定期报告（季报/半年报/年报）一经发布，必须立即纳入相关因子分析，不得沿用上一期数据。报告须标注所用财报期。
14. **单位经济镜头必须执行**：第 1.5 步（单位经济与量价拆分）不可跳过。即使公司无理想"单位"指标，也须以可获得的量价维度替代并说明，以捕捉"增收不增利/单位盈利侵蚀"信号。
15. **每因子必须三时间框架判定（强制）**：当前多空 + 中期展望（3-6 个月）+ 长期评估（3 年）三列独立给出，不可省略任何一列。即便方向高度不明朗，也须明确给出"不明朗"/"中性"并说明原因（如"无可靠前瞻信号""多空信号并存""长期基本不适用"），不可留空、不可并入其他列、不可用"维持现状"等模糊措辞替代。中期与长期判断须有依据（已公告计划/前瞻指标/趋势外推/卖方一致预期/行业空间测算等），不可凭空臆测。
16. **评分必须经脚本计算（强制）**：加权多空评分必须由 `scoring_calc.py` 按第 1.7 步计算生成，不可手工估算总分。档位先行、分数跟随：先按 factor-definitions.md 判档位，再由配置映射得分，不允许跳过档位直接"拍分数"。强度微调（±1）须在因子说明中给出依据。
17. **权重与阈值以配置为唯一权威**：因子权重、框架权重、分值映射、评级阈值均以 `config/scoring-config.json` 为准，评估过程中不得临时擅改；调整参数须修改配置文件并在其 `changelog` 中记录（日期+变更+理由），跨期对比须注明配置版本，保证回测可追溯。
18. **评分不改定性结论**：加权总分是定性判断的汇总呈现。当总分与定性分析明显矛盾（如框架间严重背离）时，以定性分析为准并在报告中标注背离，不得用分数掩盖分歧。
19. **实时时间锚定（强制最先执行）**：任何评估与网络搜索前，必须先用命令获取真实系统日期作为"评估基准日"，严禁用模型记忆的日期或上一会话遗留日期；所有搜索词须携带该实时年份 / 月份，确保命中最新财报与最新进展，不得用过期时间搜索（见前置步骤）。
20. **最新财报状态须实时确认**：公司最新一期定期报告（年报 / 一季报 / 中报 / 三季报）的报告期与披露日期，必须基于评估基准日通过实时网络搜索确认，严禁凭模型记忆或假设"数据即最新"；以此为准驱动财务数据相关的因子评估与第 2 步数据时效（呼应纪律第 13 条"季报发布即刷新"）。
21. **熔断优先于评分（强制）**：第 1.6 步四项熔断检查（偿债红线/审计与治理/大股东质押/客户集中度）不可跳过；触发任一项综合评级封顶"看空"，不得以其他因子利多抵消，触发项与证据须在报告显著位置标注。
22. **预期差必须对照一致预期（强制）**：因子 13 评估必须执行 13a 预期差校验，将业绩方向判断与卖方一致预期对比；本评估弱于一致预期时须在结论中标注"不及预期风险"，避免"方向判对但股价已定价更高预期"的亏损场景。
23. **估值必须分类型判定（强制）**：因子 14 须先判定公司类型（成长/价值/周期）再选主估值指标（成长→PEG/PS，价值→PE/股息率，周期→PB）；禁止对周期股用 PE 分位作主锚（周期顶部低 PE 是陷阱）、对未盈利成长股用 PE；估值结论须与 13a 预期差结论一致，背离时须说明。
24. **前置关卡一票否决（强制）**：第 0.5 步五道前置筛选关卡不可跳过、不可调换顺序；任一关卡触发即终止全部流程，不得用其他因子利多或后续评分"挽救"继续；触发时只输出终止说明，不生成完整评估报告（用户明确要求时除外）。

## 资源文件

| 文件 | 用途 |
|------|------|
| [references/factor-definitions.md](./references/factor-definitions.md) | 14 因子的多/空判定标准与所需数据（必读） |
| [references/momentum-check.md](./references/momentum-check.md) | 因子 13（业绩动能）的 14 个检查项定义与预警规则（必读） |
| [references/scoring-algorithm.md](./references/scoring-algorithm.md) | 加权多空评分算法规格：分值映射/权重/阈值/长期判定指引（必读） |
| [references/data-sources.md](./references/data-sources.md) | 结构化因子的 WeStock/NeoData 数据获取命令 |
| [references/source-credibility.md](./references/source-credibility.md) | T0-T5 信源可信度分级标准 |
| [scripts/momentum_calc.py](./scripts/momentum_calc.py) | 业绩动能（因子 13）自动化计算脚本（仅依赖标准库） |
| [scripts/scoring_calc.py](./scripts/scoring_calc.py) | 三时间框架加权评分自动化计算脚本（仅依赖标准库） |
| [config/scoring-config.json](./config/scoring-config.json) | 评分参数唯一权威配置：因子权重/框架权重/档位分值/评级阈值 |
| [assets/report-template.md](./assets/report-template.md) | .md 评估报告输出模板 |
