---
name: stock-bull-bear-evaluator
description: 股票多空因子评估专家。当用户输入一个公司名或股票名（如"比亚迪"、"腾讯控股"、"分析XX的多空"），对该公司进行 13 个因子的系统性多空评估，并针对利空因子寻找公司应对措施、定义可量化跟踪指标、通过网络搜索获取最新进展并给出评价（超出预期/符合预期/不及预期）。覆盖 A 股与港股。触发场景：用户说"评估XX的多空""XX多空分析""分析XX股票多空因子""多空评估：公司名"等。
agent_created: true
---

# 股票多空因子评估专家

## 概述

对用户输入的公司/股票，系统性评估 13 个因子的多空倾向（三时间框架：当前 / 未来 3-6 个月 / 未来 3 年），完成业绩动能交叉校验（检测结构性判断与短期业绩的背离），针对利空因子深挖公司应对措施与可量化跟踪指标，并通过网络搜索验证指标最新进展。输出对话内多空评估表 + 业绩动能校验表 + 利空应对指标跟踪表 + 加权多空评分表（1-10 分制、三时间框架加权、五档评级），同时生成完整 .md 评估报告文件。

**适用市场**：A 股（sh/sz/bj）、港股（hk）。

## 触发与意图识别

触发词示例：
- "多空评估：比亚迪" / "评估腾讯控股的多空"
- "分析 XX 股票的多空因子"
- "XX 多空分析" / "帮我看下 XX 的多空"

输入为公司名或股票名时，按本 skill 流程执行。若用户只问单一指标（如"比亚迪融资余额多少"），不触发本 skill，直接用对应数据源回答。

## 工作流程

### 第 0 步：解析标的与市场

1. 用 WeStock 搜索解析公司名 → 股票代码：
   ```bash
   npx -y westock-data-clawhub@1.0.4 search 比亚迪
   ```
2. 确认代码前缀：A 股 `sh/sz/bj`，港股 `hk`。
3. 用 `profile` 取公司简况（行业、主营、上市日期），作为后续分析基础。

### 第 1 步：13 因子三时间框架多空评估

对下表 13 个因子逐一进行**三时间框架评估**，三个判断独立给出，不可互相替代：

- **当前多空**：基于当前事实判断，取值 `利多 / 利空 / 中性 / 中性偏多 / 中性偏空`
- **中期展望**：基于**未来 3-6 个月**该因子的变动方向判断，取值 `向好 / 不明朗 / 不佳`
- **长期评估**：基于**未来 3 年**该因子的方向判断，取值 `利多 / 利空 / 中性 / 中性偏多 / 中性偏空`（判定指引见 [references/scoring-algorithm.md](./references/scoring-algorithm.md) 第 5 节；筹码/资金/舆论等短期因子长期基本不适用，默认"中性"）
  - **向好（多）**：未来 3-6 个月该因子方向明显改善（不关注绝对值，仅关注方向）
  - **不明朗（中性）**：未来方向不确定，或无可靠前瞻信号，或多空信号并存无法判断
  - **不佳（空）**：未来 3-6 个月该因子方向明显恶化

**为什么选 3-6 个月**：该窗口与季报披露节奏匹配（1-2 个季度），可被后续财报验证，避免过度推断；同时也与第 2 步业绩动能交叉校验的"1-3 季度战术性扰动"窗口对齐，便于交叉印证。

**多框架并列的典型场景**（合法且常见，须如实标注）：
- 当前利多 + 中期展望不佳：景气仍在高位但拐点临近（如产能即将集中释放）
- 当前利空 + 中期展望向好：底部已现、拐点将至（如库存去化接近尾声）
- 当前中性 + 中期展望不明朗：无可靠前瞻信号时如实标注，不强行判断
- 中期承压 + 长期利多：短期扰动不改 3 年结构性方向（如周期底部）

每个因子附 1-2 句简要说明（含关键数据或事件），需**同时覆盖当前事实依据与中期/长期判断依据**。

**因子定义与三时间框架判定标准**：见 [references/factor-definitions.md](./references/factor-definitions.md)（必读，包含每个因子的当前多/空标准 + 中期向好/不佳标准与所需数据）；长期（3 年）判定指引见 [references/scoring-algorithm.md](./references/scoring-algorithm.md) 第 5 节。

**数据源策略**（混合数据源）：
- **定性因子**（宏观/社会舆论/行业/政策/上游/下游/竞争对手/替代品/进入壁垒）：用元宝搜索标准版（`tencent-yuanbao-standard-search` skill），不可用时回退 WebSearch。
- **结构化因子**（公司资本操作/筹码/资金/估值）：用 WeStock CLI（`npx -y westock-data-clawhub@1.0.4`）取结构化数据，具体命令见 [references/data-sources.md](./references/data-sources.md)。
- **机构观点/研报**：用 NeoData 金融搜索（`neodata-financial-search` skill）补充。

**信源可信度**：所有结论引用 T2+ 信源，T4/T5 不引用。标准见 [references/source-credibility.md](./references/source-credibility.md)。

### 第 1.5 步：单位经济与量价拆分（通用分析镜头，贯穿第 1-2 步）

在定性因子评估与动能校验之间，强制执行一次**行业无关的单位经济拆解**，作为连接"结构判断"与"财务数据"的桥梁：

1. **识别核心单位**：为该公司选定一个最具代表性的"单位"（如产品件/吨/千瓦时/订阅用户/订单/客单/座位/房间夜等），由分析者根据公司业务自行确定，框架不预设。
2. **量价拆分**：将收入增长分解为"量（出货/销量/用户数）× 价（单价/ARPU/客单价）"两部分，识别增长由量驱动还是价驱动，以及量增价跌/量价齐升等组合含义。
3. **单位盈利趋势**：跟踪"单位毛利""单位净利"的趋势，捕捉"总收入/总利润增长但单位盈利被侵蚀"的隐蔽恶化（绝对额增长 ≠ 单位盈利健康）。
4. **传导校验**：当上游涨价时，用单位售价 vs 单位成本的变动判断传导是否充分（联动可传导 vs 难传导 + 锁价敞口，见因子 5）。

**通用性原则**：本镜头不绑定任何行业或公司。若某公司确无合理"单位"指标（罕见），则标注"单位经济不适用"并以"收入=量×价"中可获取的维度替代，不得跳过分析。

### 第 1.6 步：三时间框架加权评分

完成第 1 步因子判定后，将各因子的三框架档位代入加权打分模型，量化为 1-10 分制加权总分与五档评级（强烈看多/看多/中性/看空/强烈看空）。算法规格（分值映射、因子权重、框架权重、阈值）见 [references/scoring-algorithm.md](./references/scoring-algorithm.md)，全部参数集中于 [config/scoring-config.json](./config/scoring-config.json)。

**自动化脚本（强制使用）**：

```bash
python3 scripts/scoring_calc.py <评估输入.json>
```

输入 JSON 为 13 因子的三框架档位（格式见 scoring-algorithm.md 第 6.2 节），脚本自动校验配置（权重和=100%、档位完整、阈值覆盖）、计算各框架得分与综合加权总分、输出可直接嵌入报告的 markdown 评分表。强度微调（档位基准分 ±1）为可选项，须给出依据；回测时关闭微调保证一致性。

**与后续步骤的关系**：评分基于第 1 步定性判断，第 2 步交叉校验的预警等级**不直接扣分**（避免双重计入），但在第 5 步输出中与评分并列呈现。

### 第 2 步：业绩动能交叉校验

完成 13 因子多空评估后，对最新季度财务数据进行趋势交叉校验，检测结构性判断与短期业绩之间是否存在背离。

**检查项**（基于多季度趋势，非单季度绝对值）：见 [references/momentum-check.md](./references/momentum-check.md)（必读，包含 14 个检查项的定义与预警规则）。

**核心检查项**：
1. 营收增长动能趋势（YoY 连续减速 / 由正转负）
2. 扣非净利润趋势（排除非经常性损益后的核心盈利）
3. 盈利质量（归母NP 与扣非NP 差额占比）
4. 经营现金流趋势（现金流转负 / 与利润背离）
5. 存货周期趋势（库存积压 / 周转放缓）
6. 总资产周转率趋势
7. 存货周转率趋势
8. 应收账款周转率趋势
9. 流动比率趋势
10. 速动比率趋势
11. 增收不增利检测（量利剪刀差：营收YoY − 扣非NP YoY）
12. 资产减值趋势（减值/营收占比，风险释放/盈利质量）
13. 合同负债/预收趋势（前瞻在手订单）
14. 存货积压风险（存货增速 vs 营收增速 + 结构校验）

**自动化脚本**（推荐）：

```bash
python3 scripts/momentum_calc.py <stock_code>
```

脚本自动获取三大报表、计算所有指标与比率、检测趋势、输出预警报告。仅依赖 Python 标准库。若脚本不可用，手动从 WeStock `finance` 命令获取数据并按 momentum-check.md 定义的规则计算。

**预警等级**：正常 / 关注 / 预警 / 严重预警。仅"预警"和"严重预警"影响最终结论（标注背离）。

**对后续步骤的影响**：若交叉校验触发"预警"或"严重预警"，第 3 步中相关利空因子的跟踪指标**必须使用最新季度数据**（非 trailing annual）。

### 第 3 步：利空因子应对措施与跟踪指标

对所有判定为**利空 / 中性偏空**的因子：
1. 在公司公开信息（年报、公告、投资者交流、官方 PR）中查找是否有**明确的应对措施**。
2. 若有应对措施 → 定义**可量化跟踪指标**，格式：`{指标}：指标名称`。
3. 若无应对措施 → **如实报告"公司未公开明确应对措施"**，不编造。
4. **数据时效性规则**：若第 2 步交叉校验触发了预警，则本步中相关利空因子的跟踪指标必须使用**最新季度数据**（非 trailing annual），并在指标旁标注数据日期。

### 第 4 步：跟踪指标最新进展查询

对第 3 步定义的每个跟踪指标：
1. 用元宝搜索标准版（不可用回退 WebSearch）查询该指标最新进展。
2. 给出**进展评价**：超出预期 / 符合预期 / 不及预期。
3. 评价必须基于具体数据或事件，附信源链接。

**搜索纪律**：每个指标单独搜索，不合并查询；查询词要具体（如"比亚迪海外销量 2026"而非"比亚迪海外"）；优先 T2+ 信源。

### 第 5 步：输出

#### 5.1 对话内输出（四张表）

**表 1：13 因子三时间框架多空评估表**

| 因子 | 当前多空 | 中期展望(3-6M) | 长期评估(3Y) | 简要说明 |
|------|----------|----------------|--------------|----------|
| 宏观 | 利多/利空/中性 | 向好/不明朗/不佳 | 利多/利空/中性 | ... |
| ... | ... | ... | ... | ... |

**表 2：业绩动能交叉校验表**

| 检查项 | 趋势 | 预警等级 | 说明 |
|--------|------|----------|------|
| 营收增长动能趋势 | declining/volatile/improving | 正常/关注/预警/严重预警 | ... |
| ... | ... | ... | ... |

（若全部为"正常"，可简化为一行说明。）

**表 3：利空应对指标跟踪表**

| 利空因子 | 公司应对措施 | 跟踪指标 | 最新进展 | 进展评价 |
|----------|--------------|----------|----------|----------|
| 下游 | 技术升级提升渗透率 | 闪充站建设目标达成率 | 截至 X 月建成 X 个 | 符合预期 |
| ... | ... | ... | ... | ... |

**表 4：加权多空评分表**（由 `scoring_calc.py` 自动生成，直接粘贴）

含：各时间框架的因子分值×权重明细、三框架得分与框架评级、综合加权总分与综合评级（五档）。对话内可只贴"综合加权总分"汇总表 + 简要框架得分，完整明细进报告文件。

#### 5.2 生成 .md 报告文件

在当前工作目录生成 `多空评估报告_<公司名>_<YYYYMMDD>.md`，使用 [assets/report-template.md](./assets/report-template.md) 模板。报告含：标的概况、13 因子三框架详评、加权多空评分、业绩动能交叉校验、利空应对与指标跟踪、综合多空判断、信源清单、免责声明。

## 关键纪律

1. **每个因子必须给明确多空判断**，不可只列现象不下结论。
2. **利空因子必须有应对措施分析**：有则给指标，无则如实报告"未公开明确应对措施"。
3. **跟踪指标进展评价必须基于具体数据/事件**，不可泛泛而谈。
4. **信源 T2+ 才可引用**，T4/T5（雪球、贴吧、KOL）不引用。
5. **港股特殊性**：筹码数据 WeStock 不支持（仅沪深京 A 股），港股筹码多空用 web search 查机构持仓变化；港股资金多空的 call/put 用 web search 查。
6. **估值水位**：PB 历史分位优先用 NeoData 或 web search 查"PB 历史分位"；若无直接数据，用 WeStock finance 取净资产 + K 线取价格，按季度计算近 5 年 PB 序列后求分位。
7. **数据时效**：所有数据标注日期；新闻类信源优先近 3 个月。
8. **中立立场**：多空判断基于事实，不预设立场；利多利空并存时如实标注。
9. **不构成投资建议**：报告末尾必须附免责声明。
10. **货币单位**：港股数据为港元/美元，展示时标注正确货币单位，A 股为人民币。
11. **业绩动能交叉校验必须执行**：13 因子评估完成后，必须运行第 2 步交叉校验。不可跳过，也不可将校验结果简单并入因子 2（社会舆论）。
12. **趋势优先于绝对值**：交叉校验中所有检查项以多季度（至少 3-4 季度）变化趋势为判断依据，不依赖单季度绝对值。单季度异常仅标"关注"，连续 2-3 季度同方向恶化才触发"预警"或"严重预警"。
13. **财务比率不设绝对阈值**：总资产周转率/存货周转率/应收账款周转率/流动比率/速动比率仅看趋势方向（改善/恶化/稳定），不设绝对阈值（行业差异大）。
14. **跟踪指标时效性规则**：若交叉校验触发"预警"或"严重预警"，第 3 步中相关利空因子的跟踪指标必须使用最新季度数据（非 trailing annual），并在指标旁标注数据日期。
15. **结构性判断与短期业绩背离时必须标注**：当 13 因子判断为"利多/中性偏多"但交叉校验触发"预警"或"严重预警"时，必须在最终结论中明确标注"结构性利多与短期业绩存在背离"，并建议跟踪后续季度业绩是否企稳。
16. **框架通用性（强制）**：本框架适用于 A 股/港股任意行业。所有因子定义、检查项、分析镜头均不得绑定特定公司或行业的方法。涉及"单位""产能""供给能力指标"等概念时，由分析者按公司业务性质对应替换（如产能→人员/算力/门店等），框架不预设行业专属指标。
17. **政策因子必须双向分列**：政策因子须同时列出支持项与打压项（含税费新增/恢复征收、补贴退坡、监管收紧等），分别评估后汇总。严禁只抓一端而漏掉反向政策（如只见反内卷利多而忽略同期税费利空）。
18. **季报发布即刷新**：最新一期定期报告（季报/半年报/年报）一经发布，必须立即纳入业绩动能交叉校验与相关因子分析，不得沿用上一期数据。报告须标注所用财报期。
19. **单位经济镜头必须执行**：第 1.5 步（单位经济与量价拆分）不可跳过。即使公司无理想"单位"指标，也须以可获得的量价维度替代并说明，以捕捉"增收不增利/单位盈利侵蚀"信号。
20. **每因子必须三时间框架判定（强制）**：当前多空 + 中期展望（3-6 个月）+ 长期评估（3 年）三列独立给出，不可省略任何一列。即便方向高度不明朗，也须明确给出"不明朗"/"中性"并说明原因（如"无可靠前瞻信号""多空信号并存""长期基本不适用"），不可留空、不可并入其他列、不可用"维持现状"等模糊措辞替代。中期与长期判断须有依据（已公告计划/前瞻指标/趋势外推/卖方一致预期/行业空间测算等），不可凭空臆测。
21. **评分必须经脚本计算（强制）**：加权多空评分必须由 `scoring_calc.py` 按第 1.6 步计算生成，不可手工估算总分。档位先行、分数跟随：先按 factor-definitions.md 判档位，再由配置映射得分，不允许跳过档位直接"拍分数"。强度微调（±1）须在因子说明中给出依据。
22. **权重与阈值以配置为唯一权威**：因子权重、框架权重、分值映射、评级阈值均以 `config/scoring-config.json` 为准，评估过程中不得临时擅改；调整参数须修改配置文件并在其 `changelog` 中记录（日期+变更+理由），跨期对比须注明配置版本，保证回测可追溯。
23. **评分不改定性结论**：加权总分是定性判断的汇总呈现。当总分与定性分析明显矛盾（如框架间严重背离）时，以定性分析为准并在报告中标注背离，不得用分数掩盖分歧。交叉校验预警不直接扣分，与评分并列呈现。

## 资源文件

| 文件 | 用途 |
|------|------|
| [references/factor-definitions.md](./references/factor-definitions.md) | 13 因子的多/空判定标准与所需数据（必读） |
| [references/momentum-check.md](./references/momentum-check.md) | 业绩动能交叉校验的 14 个检查项定义与预警规则（必读） |
| [references/scoring-algorithm.md](./references/scoring-algorithm.md) | 加权多空评分算法规格：分值映射/权重/阈值/长期判定指引（必读） |
| [references/data-sources.md](./references/data-sources.md) | 结构化因子的 WeStock/NeoData 数据获取命令 |
| [references/source-credibility.md](./references/source-credibility.md) | T0-T5 信源可信度分级标准 |
| [scripts/momentum_calc.py](./scripts/momentum_calc.py) | 业绩动能交叉校验自动化计算脚本（仅依赖标准库） |
| [scripts/scoring_calc.py](./scripts/scoring_calc.py) | 三时间框架加权评分自动化计算脚本（仅依赖标准库） |
| [config/scoring-config.json](./config/scoring-config.json) | 评分参数唯一权威配置：因子权重/框架权重/档位分值/评级阈值 |
| [assets/report-template.md](./assets/report-template.md) | .md 评估报告输出模板 |
